<!-- META: {"nodeId": "6LeBq413JA0w1YkYC3O0bowLVDOnGvpb", "title": "在科情HR系统中考勤模块中，会出现日期格式计算间隔时间问题", "docUrl": "https://alidocs.dingtalk.com/i/nodes/6LeBq413JA0w1YkYC3O0bowLVDOnGvpb?utm_scene=team_space", "path": "/自研/数据错误问题/在科情HR系统中考勤模块中，会出现日期格式计算间隔时间问题", "fetchTime": "2026-08-13 23:55:31"} -->

## 问题现象

在科情HR系统中考勤模块中，会出现日期格式计算间隔时间问题

## 问题原因

在JS中，一般日期字段是字符串，需先将日期字符串转换为Date后，再进行相关计算；

## 解决方案

通过该JS方法，进行计算
//计算两个日期之差
function GetDateDiff(startTime, endTime, diffType) \{
 //将xxxx-xx-xx的时间格式，转换为 xxxx/xx/xx的格式
 startTime = startTime.replace(/-/g, "/");
 endTime = endTime.replace(/-/g, "/");
 //将计算间隔类性字符转换为小写
 diffType = diffType.toLowerCase();
 var sTime = new Date(startTime); //开始时间
 var eTime = new Date(endTime); //结束时间
```
if (sTime > eTime) {
        alert("开始时间不能大于结束时间!!!");
        return false;
    }
//作为除数的数字
var divNum = 1;
switch (diffType) {
case "second":
    divNum = 1000;
    break;
case "minute":
    divNum = 1000 * 60;
    break;
case "hour":
    divNum = 1000 * 3600;
    break;
case "day":
    divNum = 1000 * 3600 * 24;
    break;
default:
    break;
}
return parseInt((eTime.getTime() - sTime.getTime()) / parseInt(divNum));

```

\}

---

## 附加信息

**对应版本**: 科情HR

**问题类型**: 数据错误问题

**解决方案类型**: 代码修改

**技术栈**: JS, JavaScript

**问题关键字**: 日期格式

**单据编号**: FX-20221203-014

**提交人**: 张柳青  \|  **提交部门**: 需求&KA部  \|  **提交日期**: 2022-12-03
