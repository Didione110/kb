<!-- META: {"nodeId": "mExel2BLV5NzBpEptPakk7xzJgk9rpMq", "title": "收银时需要同时打印医保结算单以及小票", "docUrl": "https://alidocs.dingtalk.com/i/nodes/mExel2BLV5NzBpEptPakk7xzJgk9rpMq?utm_scene=team_space", "path": "/自研/实施问题/收银时需要同时打印医保结算单以及小票", "fetchTime": "2026-08-13 23:56:41"} -->

## 问题现象

收银时需要同时打印医保结算单以及小票

## 问题原因

收银时需要同时打印医保结算单以及小票

## 解决方案

修改保护代码中的sys\_printReceipt函数
ds\_mast.field("k\_isybdy").value = "N";
if(!Utility.isSpace(ds\_mast.field("k\_PosTraceNumber").value))\{
	ds\_mast.field("k\_isybdy").value = "Y";
\}
if (ds\_mast.field("k\_isybdy").value == "Y") \{
	Printer.print();
 ds\_mast.field("k\_isybdy").value = "N"
\}
if (ds\_mast.field("k\_isybdy").value == "N") \{
	Printer.print();
\}
打印设计分类方案中增加：
if(ds\_mast.field("k\_isybdy").value == "Y" 
 && ds\_mast.field("ywlx").value != "TH")\{
 return "4phy583mbd636c6l" //医保结算
 \}
 if(ds\_mast.field("k\_isybdy").value == "Y" 
 && ds\_mast.field("ywlx").value == "TH")\{
 return "3wl6g9na9a7ulkbx" //医保撤销
 \}else

---

## 附加信息

**对应版本**: 医保接口

**问题类型**: 实施问题

**解决方案类型**: 代码修改

**技术栈**: JavaScript, ZyPrint

**技术关键词**: 追溯码, 打印

**问题关键字**: 医保打印

**单据编号**: FX-20241207-011

**提交人**: 童光  \|  **提交部门**: 实施4部  \|  **提交日期**: 2024-12-07
