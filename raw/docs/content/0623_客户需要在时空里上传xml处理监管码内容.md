<!-- META: {"nodeId": "QOG9lyrgJPEzvGyGhv511Y4l8zN67Mw4", "title": "客户需要在时空里上传xml处理监管码内容", "docUrl": "https://alidocs.dingtalk.com/i/nodes/QOG9lyrgJPEzvGyGhv511Y4l8zN67Mw4?utm_scene=team_space", "path": "/时空/应用操作/客户需要在时空里上传xml处理监管码内容", "fetchTime": "2026-08-14 00:02:58"} -->

## 问题现象

客户需要在时空里上传xml处理监管码内容

## 问题原因

时空KSOA/V10/11/12均无内置功能以及方法处理xml

## 解决方案

以KSOA为例，在表单顶部引入标签

var result = loadXML(fileInput.value);
 //SKDBedit1.value = result;
 if (result) \{
 var ret = JSON.parse(result); 
```
    var eventsNode = ret.content.children[0];
    if (!eventsNode || !eventsNode.children || eventsNode.children.length === 0) {
        alert("Î´ÕÒµ½ Event ½Úµã");
        return;
    }
    var eventNode = eventsNode.children[0]; // Events ÏÂµÄµÚÒ»¸ö Event

    // ¶¯Ì¬²éÕÒ DataField ½Úµã
    var dataFieldNode = findChildByTag(eventNode, 'DataField');
    if (!dataFieldNode || !dataFieldNode.children || dataFieldNode.children.length === 0) {
        alert("Î´ÕÒµ½ Data ½Úµã");
        return;
    }

    var dataNodes = dataFieldNode.children; // Data ½ÚµãÊý×é

```

以智友V10、11、12为例
function uf\_test\_import() \{
 var fileInput = document.createElement('input');
 fileInput.type = 'file';
 fileInput.style.display = 'none';
 fileInput.accept = '.xml';
 document.body.appendChild(fileInput);
 fileInput.addEventListener('change', function() \{
 if (this.files && this.files\[0\]) \{
 var file = this.files\[0\];
 var isXml = file.name.toLowerCase().endsWith('.xml');
 if (isXml) \{
 var reader = new FileReader();
 reader.onload = function(e) \{
 var fileContent = e.target.result; // XML内容
// console.log('XML内容:', fileContent);
 var jsonObj = xmlToJson(fileContent);
 var jsonString = JSON.stringify(jsonObj, null, 2);
```
                console.log('JSON结果:', jsonString);
                form.alert('XML已转换为JSON！查看控制台获取结果。');
     
            };
            reader.readAsText(file); // 读取为文本，也可改用 readAsBinaryString 等
            textbox14.setValue(file.name); // 只显示文件名，更友好
        } else {
            form.alert("请选择XML格式的文件！");
            textbox14.setValue("");
        }
    }
    document.body.removeChild(fileInput);
});

fileInput.click();

```

\}

function xmlToJson(xml) \{
 const parser = new DOMParser();
 const xmlDoc = parser.parseFromString(xml, 'text/xml');
```
function parseNode(node) {
    const obj = {};
    
    // 处理节点属性
    if (node.attributes) {
        for (let i = 0; i < node.attributes.length; i++) {
            const attr = node.attributes[i];
            obj[attr.name] = attr.value;
        }
    }
    
    // 处理子节点
    if (node.children && node.children.length > 0) {
        const childNames = {};
        for (let i = 0; i < node.children.length; i++) {
            const childName = node.children[i].nodeName;
            childNames[childName] = (childNames[childName] || 0) + 1;
        }
        
        for (let i = 0; i < node.children.length; i++) {
            const child = node.children[i];
            const childName = child.nodeName;
            const childValue = parseNode(child);
            
            // 如果子节点只有文本内容，直接使用文本值
            if (typeof childValue === 'string') {
                if (childNames[childName] > 1) {

                    if (!obj[childName]) obj[childName] = [ ];

                    obj[childName].push(childValue);
                } else {
                    obj[childName] = childValue;
                }
            }

```

---

## 附加信息

**对应版本**: 时空智友

**对应模块**: 供应链管理系统

**问题类型**: 应用操作

**解决方案类型**: 代码修改

**技术栈**: JavaScript

**技术关键词**: 接口, 工作流

**问题关键字**: 监管码上传

**单据编号**: FX-20250605-017

**提交人**: 汪松  \|  **提交部门**: 经理办  \|  **提交日期**: 2025-06-05
