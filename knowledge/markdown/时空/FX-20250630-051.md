# 来源数据为时间戳，需要转换成标准日期格式

> 知识条目 | 可被客服机器人引用

## 元信息

| 字段 | 值 |
|------|----|
| 知识ID | FX-20250630-051 |
| 产品线 | 时空 |
| 产品系列 | 时空 |
| 版本 | 时空智友 |
| 模块 | RM模块 |
| 问题类型 | 产品功能问题 |
| 解决方案类型 | 代码修改 |
| 技术栈 | JavaScript |
| 技术关键词 |  |
| 问题关键字 | 时间转换 |
| 分类路径 | 时空 / 产品功能问题 |
| 提交人 | 王棒 |
| 提交部门 | 实施1部 |
| 提交日期 | 2025-06-30 |

## 问题现象

来源数据为时间戳，需要转换成标准日期格式

## 问题原因

来源数据为时间戳，需要转换成标准日期格式

## 解决方案

1.新增转换函数
//根据时间戳设置日期
function uf_setDateFromTimestamp(timestamp){
 var date = new Date(Number(timestamp)) ;
 Y = date.getFullYear() + '-';
 M = (date.getMonth()+1 < 10 ? '0'+(date.getMonth()+1) : date.getMonth()+1) + '-';
 D = (date.getDate()+1 < 10 ? '0'+(date.getDate()+1) : date.getDate()+1) + ' ';
 h = (date.getHours()+1 < 10 ? '0'+(date.getHours()+1) : date.getHours()+1) + ':';
 m = (date.getMinutes()+1 < 10 ? '0'+(date.getMinutes()+1) : date.getMinutes()+1) + ':';
 s = (date.getSeconds()+1 < 10 ? '0'+(date.getSeconds()+1) : date.getSeconds()+1);
 return Y+M+D+h+m+s ;
}
2.调用
var date = uf_setDateFromTimestamp(timestamp) ;

## 引用来源

- 来源类型: `aitable`
- 来源链接: https://alidocs.dingtalk.com/i/nodes/o14dA3GK8g2jxGpGUK3X2o95J9ekBD76
- 表格记录ID: `lMeCWxR82k`
