# v10 v11 v12在使用form.open时均存在跨域问题，即不能打开非当前应用服务器外的文件或地址

> 知识条目 | 可被客服机器人引用

## 元信息

| 字段 | 值 |
|------|----|
| 知识ID | FX-20240619-031 |
| 产品线 | 时空 |
| 产品系列 | 时空 |
| 版本 | 时空智友 |
| 模块 | RM模块 |
| 问题类型 | 产品功能问题 |
| 解决方案类型 | 代码修改, 替换文件 |
| 技术栈 | JavaScript, IE, Chrome |
| 技术关键词 | 工作流 |
| 问题关键字 | 跨域问题 |
| 分类路径 | 时空 / 产品功能问题 |
| 提交人 | 汪松 |
| 提交部门 | 服务值班组 |
| 提交日期 | 2024-06-19 |

## 问题现象

v10 v11 v12在使用form.open时均存在跨域问题，即不能打开非当前应用服务器外的文件或地址

## 问题原因

因为早期V10的环境为IE，故对跨域操作支持很不友善，系统底层代码并没有考虑到这一块，dherp之后内核变成了谷歌，对此有了底层支持，但是开发商并未添加

## 解决方案

找到form.js文件搜索关键字Form.prototype.open
找到如下代码进行选择替换即可
 //如果包含点，可能是一个jsp
 if (formid.indexOf('http://') == 0 \|\|formid.indexOf('https://') == 0 ) \{
 url = formid;
 \}else if (formid.indexOf(".jsp") \> 0 \|\| formid.indexOf(".html") \> 0 \|\| formid.indexOf(".htm") \> 0) \{
 url = this.parameters.contextpath;
 if (formid.indexOf("/") != 0) \{
 url \+= "/"
 \}
 url \+= formid;
 \} else if (action == "workflow") \{
 url = this.parameters.contextpath \+ "/" \+ formid \+ ".workflow";
 action = "modify";
 \} else \{
 url = this.parameters.contextpath \+ "/" \+ formid \+ ".form";
 \}
 if (url.indexOf("?") == -1) \{
 url \+= "?";
 \} else if (url.lastIndexOf("&") != url.length - 1)

## 引用来源

- 来源类型: `doc`
- 来源链接: https://alidocs.dingtalk.com/i/nodes/ZgpG2NdyVXL3XzBzIPzvwbRYVMwvDqPk?utm_scene=team_space
- 知识库路径: `/时空/产品功能问题/v10 v11 v12在使用form.open时均存在跨域问题，即不能打开非当前应用服务器外的文件或地址`
- 表格记录ID: `1CHKP9jPYA`
