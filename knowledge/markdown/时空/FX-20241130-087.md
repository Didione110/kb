# 下单操作时，业务存在同一套，很多明细，删除只能删除当前行，可能删漏，容易误操作

> 知识条目 | 可被客服机器人引用

## 元信息

| 字段 | 值 |
|------|----|
| 知识ID | FX-20241130-087 |
| 产品线 | 时空 |
| 产品系列 | 时空 |
| 版本 | 时空智友 |
| 模块 | 供应链管理系统 |
| 问题类型 | 应用操作 |
| 解决方案类型 | 代码修改 |
| 技术栈 | JavaScript |
| 技术关键词 | 工作流 |
| 问题关键字 | 误删风险 |
| 分类路径 | 时空 / 应用操作 |
| 提交人 | 蒋志斌 |
| 提交部门 | 实施2部 |
| 提交日期 | 2024-11-30 |

## 问题现象

下单操作时，业务存在同一套，很多明细，删除只能删除当前行，可能删漏，容易误操作

## 问题原因

客户习惯键盘del按键删除，需要同一套的明细要求同时删除，并且不想用鼠标进行多选

## 解决方案

增加键盘del触发删除函数
在grid删除前和后事件挂函数进行处理
async function uf\_del\_mx\_before()\{
 ds\_ddhz.field("ClassId\_del").value = "";
 var classid = "";
 if(ds\_ddmx.getValueAt(ds\_ddmx.recno,"classid") != "")\{
 classid = ds\_ddmx.getValueAt(ds\_ddmx.recno,"classid") ;
 ds\_ddhz.field("ClassId\_del").value = classid ;
```
}

```

\}

function uf\_del\_mx\_after\_new() \{
 var classid = "";
 if (ds\_ddhz.field("ClassId\_del").value != "") \{ 
 classid = ds\_ddhz.field("ClassId\_del").value ;
```
 for (var j = ds_ddmx.recordCount - 1 ;j >= 0 ;j--) {
 if ( ds_ddmx.getValueAt(j, "classid") == classid && ds_ddmx.getValueAt(j, "classid") != "" ) {
 ds_ddmx.deleteRow(j);
 }
 }
 }

```

\}

## 引用来源

- 来源类型: `doc`
- 来源链接: https://alidocs.dingtalk.com/i/nodes/QG53mjyd809pBOwOtwNDzQPlW6zbX04v?utm_scene=team_space
- 知识库路径: `/时空/应用操作/下单操作时，业务存在同一套，很多明细，删除只能删除当前行，可能删漏，容易误操作`
- 表格记录ID: `IpWTN9ZRWR`
