# 再写js判断两个金额的大小时，js会将一个2位小数变成多为小数，而前端界面是只保留两位小数的，操作用户看着两个金额一样但是就是报错金额不相等

> 知识条目 | 可被客服机器人引用

## 元信息

| 字段 | 值 |
|------|----|
| 知识ID | FX-20240407-043 |
| 产品线 | 时空 |
| 产品系列 | 时空 |
| 版本 | 时空智友 |
| 模块 | 供应链管理系统 |
| 问题类型 | 软件无法使用 |
| 解决方案类型 | 代码修改 |
| 技术栈 | JS |
| 技术关键词 | 凭证, 审批流, 工作流 |
| 问题关键字 | 精度丢失 |
| 分类路径 | 时空 / 软件无法使用 |
| 提交人 | 刘程峰 |
| 提交部门 | 实施4部 |
| 提交日期 | 2024-04-07 |

## 问题现象

再写js判断两个金额的大小时，js会将一个2位小数变成多为小数，而前端界面是只保留两位小数的，操作用户看着两个金额一样但是就是报错金额不相等

## 问题原因

js脚本的自带函数的问题

## 解决方案

if(ds_djhz.field("PayType").value == 1){
 var sum=Math.round(ds_djhz.field("taxamount").value* 100) / 100 + Math.round(ds_djhz.field("bjtaxamount").value* 100) / 100;
 if(Math.round(jiesje * 100) / 100 < Math.round(sum* 100) / 100 ){
 //alert(jiesje+" "+sum);
 form.alert("回款认领金额小于单据金额，请检查！");
 return false;
 }
 }

将两个金额*100取整然后再/100就可以只保留两位小数了，同理比较其他多位小数调整计算倍数即可

## 引用来源

- 来源类型: `aitable`
- 来源链接: https://alidocs.dingtalk.com/i/nodes/o14dA3GK8g2jxGpGUK3X2o95J9ekBD76
- 表格记录ID: `vjvHcB8x5O`
