---
{
  "title": "bom查出未展现出树形结构级联关系状态",
  "kbId": "FX-20240802-004",
  "productLine": "用友",
  "productSeries": "U9C",
  "version": "U9C",
  "module": "供应链",
  "problemType": "数据错误问题",
  "solutionTypes": [
    "代码修改",
    "数据库操作"
  ],
  "techStack": [
    "Java"
  ],
  "techKeywords": [
    "凭证",
    "审批流",
    "库存",
    "追溯码"
  ],
  "keywords": [
    "树形结构"
  ],
  "category": [
    "用友",
    "数据错误问题"
  ],
  "submitter": "肖博伦",
  "department": "研发1部",
  "date": "2024-08-02",
  "sourceType": "doc",
  "sourceUrl": "https://alidocs.dingtalk.com/i/nodes/Obva6QBXJwAaqRPRcMA3lrgeWn4qY5Pr?utm_scene=team_space",
  "sourcePath": "/用友/数据错误问题/bom查出未展现出树形结构级联关系状态",
  "status": "active",
  "updatedAt": "",
  "outDir": "用友/数据错误问题"
}
---

# bom查出未展现出树形结构级联关系状态

> 知识条目 · 可被客服机器人引用

## 元信息

| 字段 | 值 |
|------|----|
| 知识ID | `FX-20240802-004` |
| 产品线 | 用友 |
| 产品系列 | U9C |
| 版本 | U9C |
| 模块 | 供应链 |
| 问题类型 | 数据错误问题 |
| 解决方案类型 | 代码修改, 数据库操作 |
| 技术栈 | Java |
| 技术关键词 | 凭证, 审批流, 库存, 追溯码 |
| 问题关键字 | 树形结构 |
| 分类路径 | 用友 / 数据错误问题 |
| 提交人 | 肖博伦 |
| 提交部门 | 研发1部 |
| 提交日期 | 2024-08-02 |
| 状态 | active |

## 问题现象

bom查出未展现出树形结构级联关系状态

## 问题原因

关联条件未找对

## 解决方案

使用递归创建树形结构并算出母节点下所有子节点价格并汇中到母节点上
List\&lt;BOM\_D\&gt; children = getChildren(bom\_ds1, b.getItemMaster());
 lg.info("children:\&#123;\&#125;",JSONObject.toJSONString(children));
 for (BOM\_D child : children)

DecimalFormat decimalFormat = new DecimalFormat("#.#####");
```
public Double sumPrice(BOM_D children)&#123;
 Double sun = 0D;
 if (children.getIsLeaf())&#123;
 List&lt;BOM_D&gt; d = children.getD();
 for (BOM_D bom_d : d) &#123;
 sun+=sumPrice(bom_d);
 &#125;
 &#125;else &#123;
 String priceTC = children.getPriceTC();
 if (!StringUtils.isEmpty(priceTC))&#123;
 sun+=Double.parseDouble(priceTC);
 &#125;
 &#125;
 String format = decimalFormat.format(sun);
 children.setPriceTC(format);

```

// db
 insert\_d(children);
 return sun;
 \&#125;
```
public static List&lt;BOM_D&gt; getChildren(List&lt;BOM_D&gt; beanList,String parentId)&#123;
 //创建返回树结构数据集合
 List&lt;BOM_D&gt; treeList = new ArrayList&lt;&gt;();
 for (BOM_D bean : beanList) &#123;
 //创建当前节点的bean
 BOM_D treeBean = new BOM_D();
 //创建当前节点下子节点的数据集合
 List&lt;BOM_D&gt; childrenList = new ArrayList&lt;&gt;();
 //如果当前节点等于【参数】父节点，则进行递归操作
 if (parentId.equals(bean.getPnote()))&#123;
 //将节点数据复制到当前bean下，也就是上面创建的【treeBean对象】
 BeanUtils.copyProperties(bean,treeBean);
 //递归查询当前节点是否有子节点
 childrenList = getChildren(beanList,bean.getItemMaster());
 //有则将子节点的数据集合添加到当前bean下,无节点则打上标识
 if (childrenList.size()&gt;0)&#123;
 treeBean.setD(childrenList);
 treeBean.setIsLeaf(true);
 &#125;else &#123;
 treeBean.setIsLeaf(false);
 &#125;
 //将当前构建的树对象放入返回集合中
 treeList.add(treeBean);
 &#125;
 &#125;
 return treeList;
&#125;

```

## 引用来源

- 来源类型: `doc`
- 来源链接: https://alidocs.dingtalk.com/i/nodes/Obva6QBXJwAaqRPRcMA3lrgeWn4qY5Pr?utm_scene=team_space
- 知识库路径: `/用友/数据错误问题/bom查出未展现出树形结构级联关系状态`
- 表格记录ID: `3u5f9c3CCy`
